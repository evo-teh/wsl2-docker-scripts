version: '3.8'
services:
    rabbitmq:
        container_name: ${containerName}
        volumes:
            - '${volumePath}:/var/lib/rabbitmq'
        ports:
            - '${hostPort}:5672'
            - '${uiPort}:15672'
            - '${prometheusPort}:25672'
        image: ${image}